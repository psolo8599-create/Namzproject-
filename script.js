const urlInput = document.getElementById("url");const logoInput = document.getElementById("logo");const buildBtn = document.getElementById("buildBtn");const previewBox = document.getElementById("previewBox");const previewImg = document.getElementById("previewImg");const statusHint = document.getElementById("statusHint");// Preview LogologoInput.addEventListener("change", () => {    const file = logoInput.files[0];    if (!file) return;    const reader = new FileReader();    reader.onload = () => {        previewImg.src = reader.result;        previewBox.style.display = "block";    };    reader.readAsDataURL(file);});// Build APKbuildBtn.addEventListener("click", async () => {    const siteURL = urlInput.value.trim();    if (!siteURL) return alert("Masukkan URL dulu!");    statusHint.textContent = "Mengirim data ke server...";    const formData = new FormData();    formData.append("url", siteURL);    if (logoInput.files[0]) formData.append("logo", logoInput.files[0]);    try {        const res = await fetch("/api/build", {            method: "POST",            body: formData        });        const text = await res.text();        let json;        try {            json = JSON.parse(text);        } catch {            alert("Server mengirim data tidak valid:\n" + text);            return;        }        if (json.success) {            statusHint.textContent = "APK berhasil dibuat!";            window.location.href = json.download;        } else {            statusHint.textContent = "Gagal membuat APK.";            alert(json.error);        }    } catch (err) {        alert("Kesalahan jaringan.");    }});